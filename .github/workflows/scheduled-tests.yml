name: Scheduled Daily Tests

on:
  schedule:
    # KST 07:00 = UTC 22:00 (전날)
    - cron: "0 22 * * *"

  # 수동 실행도 가능 (테스트용)
  workflow_dispatch:

permissions:
  actions: write
  contents: read

jobs:
  dispatch:
    name: Trigger Daily Test Suites
    runs-on: ubuntu-latest
    steps:
      - name: Trigger CMR (prod)
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'playwright.yml',
              ref: 'main',
              inputs: {
                suite: 'cmr',
                environment: 'prod',
                retries: '3',
                notify_slack: 'true',
              },
            });
            console.log('CMR (prod) dispatched');

      - name: Trigger AlbumBuddy (prod)
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'playwright.yml',
              ref: 'main',
              inputs: {
                suite: 'albumbuddy',
                environment: 'prod',
                retries: '3',
                notify_slack: 'true',
              },
            });
            console.log('AlbumBuddy (prod) dispatched');

      - name: Trigger Admin (STG)
        # 매일 실행으로 admin-auth.json 의 7일짜리 STG 세션 쿠키를 항상 갱신 →
        # admin run 공백이 7일을 넘겨 인증이 만료되는 데드락을 방지한다.
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'playwright.yml',
              ref: 'main',
              inputs: {
                suite: 'admin',
                environment: 'stg',
                retries: '3',
                notify_slack: 'true',
              },
            });
            console.log('Admin (STG) dispatched');
