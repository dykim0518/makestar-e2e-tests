import { test, expect } from "@playwright/test";

// Playwright Test Agents seed file
// Planner/Generator가 이 파일을 참조하여 테스트 환경을 설정합니다.
// auth.json 기반 인증 세션이 playwright.config.js에서 자동 적용됩니다.

test.describe("Seed", () => {
  test("seed", async ({ page }) => {
    // generate code here.
  });
});
